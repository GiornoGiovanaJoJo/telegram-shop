# Пошаговая инструкция по развертыванию на VPS Jino

## Данные вашего сервера

- **Хост:** `23b3a0621a6b.vps.myjino.ru`
- **IP:** `147.45.99.246`
- **Порт:** `22`
- **Логин:** `root`
- **Пароль:** `aFJ6t3dVBG^5gh`
- **ОС:** Ubuntu

---

## Шаг 1: Подключение к серверу

### В Windows (через PowerShell или CMD):

```powershell
ssh root@23b3a0621a6b.vps.myjino.ru
# или
ssh root@147.45.99.246
```

При первом подключении введите `yes` для подтверждения.

**Или используйте PuTTY:**
1. Скачайте PuTTY: https://www.putty.org/
2. Откройте PuTTY
3. Введите:
   - Host: `23b3a0621a6b.vps.myjino.ru` или `147.45.99.246`
   - Port: `22`
   - Connection type: SSH
4. Нажмите "Open"
5. Введите логин: `root`
6. Введите пароль: `aFJ6t3dVBG^5gh`

---

## Шаг 2: Загрузка файлов проекта на сервер

### Вариант 1: Через Git (рекомендуется)

Если ваш проект уже в Git репозитории:

```bash
# На сервере
cd /root
git clone https://github.com/GiornoGiovanaJoJo/telegram-shop.git
cd telegram-shop
```

### Вариант 2: Через SCP (из Windows PowerShell)

```powershell
# Из папки с проектом на вашем компьютере
scp -r * root@147.45.99.246:/root/telegram-shop/
```

### Вариант 3: Через WinSCP (графический интерфейс)

1. Скачайте WinSCP: https://winscp.net/
2. Подключитесь к серверу:
   - Host: `147.45.99.246`
   - Username: `root`
   - Password: `aFJ6t3dVBG^5gh`
3. Перетащите все файлы проекта в `/root/telegram-shop/`

---

## Шаг 3: Настройка сервера

### 3.1. Первоначальная настройка

```bash
# Перейдите в папку проекта
cd /root/telegram-shop

# Перейдите в папку с скриптами развертывания
cd vps-deploy

# Сделайте скрипты исполняемыми
chmod +x setup.sh deploy.sh

# Запустите первоначальную настройку
./setup.sh
```

Скрипт автоматически:
- Обновит систему
- Установит Node.js 18+
- Установит PM2 и Nginx
- Создаст необходимые директории
- Установит зависимости проекта

### 3.2. Настройка переменных окружения

```bash
# Вернитесь в корень проекта
cd /root/telegram-shop

# Скопируйте пример файла окружения
cp vps-deploy/env.example .env

# Откройте файл для редактирования
nano .env
```

Заполните все переменные:
```env
BOT_TOKEN=7246350542:AAG05DyqmwOsGKJd4FZv58pFdAgCl-69HeI
ADMIN_CHAT_ID=6391708212
TINKOFF_TERMINAL_KEY=1769081750030
TINKOFF_PASSWORD=d^cN_36^ukkqbwbU
TINKOFF_API_URL=https://securepay.tinkoff.ru/v2/
BASE_URL=https://ваш-домен.com
PORT=3000
NODE_ENV=production
```

**Важно:** Замените `https://ваш-домен.com` на ваш реальный домен или IP адрес.

Сохраните файл: `Ctrl+O`, затем `Enter`, затем `Ctrl+X` для выхода.

---

## Шаг 4: Настройка Nginx

### 4.1. Редактирование конфигурации

```bash
cd /root/telegram-shop/vps-deploy

# Откройте конфигурацию Nginx
nano nginx.conf
```

Замените `ваш-домен.com` на ваш домен или IP адрес:
```nginx
server_name 147.45.99.246;  # или ваш-домен.com
```

Сохраните файл.

### 4.2. Установка конфигурации Nginx

```bash
# Скопируйте конфигурацию
sudo cp nginx.conf /etc/nginx/sites-available/telegram-shop

# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/telegram-shop /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию (если есть)
sudo rm -f /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
sudo nginx -t

# Если всё ОК, перезагрузите Nginx
sudo systemctl reload nginx
```

---

## Шаг 5: Развертывание приложения

```bash
# Вернитесь в корень проекта
cd /root/telegram-shop

# Запустите скрипт развертывания
./vps-deploy/deploy.sh
```

Скрипт:
- Проверит наличие .env файла
- Установит зависимости
- Запустит приложение через PM2
- Сохранит конфигурацию PM2

---

## Шаг 6: Настройка автозапуска PM2

```bash
# Настройте автозапуск PM2 при перезагрузке сервера
pm2 startup

# Выполните команду, которую покажет PM2 (например):
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root

# Сохраните текущую конфигурацию
pm2 save
```

---

## Шаг 7: Настройка файрвола (опционально)

```bash
# Проверьте статус UFW
sudo ufw status

# Если файрвол не активен, настройте его
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS

# Активируйте файрвол
sudo ufw enable
```

---

## Шаг 8: Проверка работы

### Проверьте статус приложения:

```bash
pm2 status
pm2 logs telegram-shop
```

### Проверьте работу в браузере:

Откройте в браузере: `http://147.45.99.246`

---

## Полезные команды

### Управление приложением:

```bash
pm2 logs telegram-shop      # Просмотр логов
pm2 restart telegram-shop  # Перезапуск
pm2 stop telegram-shop      # Остановка
pm2 monit                   # Мониторинг в реальном времени
```

### Управление Nginx:

```bash
sudo systemctl status nginx    # Статус
sudo systemctl restart nginx   # Перезапуск
sudo nginx -t                  # Проверка конфигурации
```

### Обновление проекта:

```bash
cd /root/telegram-shop
git pull                      # Обновить код
./vps-deploy/deploy.sh        # Переразвернуть
```

---

## Настройка домена (опционально)

Если у вас есть домен:

1. В панели управления Jino перейдите в раздел "Домены"
2. Добавьте ваш домен
3. Настройте DNS записи:
   - A запись: `@` → `147.45.99.246`
   - A запись: `www` → `147.45.99.246`
4. Обновите BASE_URL в `.env` файле
5. Обновите `nginx.conf` с вашим доменом
6. Перезагрузите Nginx

---

## Настройка SSL (HTTPS) - опционально

Для получения бесплатного SSL сертификата через Let's Encrypt:

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx

# Получите сертификат (замените на ваш домен)
sudo certbot --nginx -d ваш-домен.com -d www.ваш-домен.com

# Certbot автоматически обновит конфигурацию Nginx
```

---

## Решение проблем

### Приложение не запускается:

```bash
# Проверьте логи
pm2 logs telegram-shop --lines 50

# Проверьте переменные окружения
cat .env

# Проверьте, что порт 3000 свободен
sudo netstat -tulpn | grep 3000
```

### Nginx не работает:

```bash
# Проверьте статус
sudo systemctl status nginx

# Проверьте логи
sudo tail -f /var/log/nginx/error.log

# Проверьте конфигурацию
sudo nginx -t
```

### Не могу подключиться по SSH:

- Проверьте, что используете правильный IP и порт
- Убедитесь, что файрвол не блокирует порт 22
- Проверьте пароль (можно сбросить в панели Jino)

---

## Безопасность

### Рекомендуется после настройки:

1. **Изменить пароль root:**
   ```bash
   passwd
   ```

2. **Создать пользователя без root прав:**
   ```bash
   adduser ваш-пользователь
   usermod -aG sudo ваш-пользователь
   ```

3. **Настроить SSH ключи** (вместо пароля)

4. **Регулярно обновлять систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## Готово!

После выполнения всех шагов ваш магазин будет доступен по адресу:
- `http://147.45.99.246` (или ваш домен)

Удачи с развертыванием! 🚀
