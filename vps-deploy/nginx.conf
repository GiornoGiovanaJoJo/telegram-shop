# Редирект HTTP на HTTPS (раскомментируйте после настройки SSL)
# server {
#     listen 80;
#     server_name ваш-домен.com www.ваш-домен.com;
#     return 301 https://$server_name$request_uri;
# }

# HTTP сервер (временно, до настройки SSL)
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    client_max_body_size 10M;
}

# HTTPS сервер (раскомментируйте после настройки SSL через Let's Encrypt)
# Certbot автоматически обновит эту конфигурацию при получении сертификата
# server {
#     listen 443 ssl http2;
#     server_name ваш-домен.com www.ваш-домен.com;
#
#     ssl_certificate /etc/letsencrypt/live/ваш-домен.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ваш-домен.com/privkey.pem;
#
#     # SSL настройки безопасности
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     client_max_body_size 10M;
# }
