# Telegram Mini App - Магазин товаров

Рабочий прототип Telegram Mini App для магазина товаров с каталогом, корзиной и оформлением заказов.

## Возможности

- 📱 Каталог товаров с категориями
- 🔍 Поиск товаров
- 🛒 Корзина покупок
- 📦 Детальная страница товара
- 💳 Оформление заказа
- 💾 Сохранение корзины в localStorage
- 🎨 Современный и адаптивный дизайн

## Структура проекта

```
прототип тг/
├── index.html      # Основная HTML структура
├── styles.css      # Стили приложения
├── app.js          # Логика приложения
└── README.md       # Инструкции
```

## Как запустить

### 🚀 Быстрый запуск (Windows)

**РЕКОМЕНДУЕТСЯ - Полный автозапуск:**
1. Дважды кликните на файл `ЗАПУСК_ВСЕГО.bat`
2. Скрипт автоматически:
   - Проверит и установит зависимости
   - Запустит сервер
   - Запустит Cloudflare Tunnel (если установлен)
   - Покажет инструкции

**Простой запуск только сервера:**
1. Дважды кликните на файл `БЫСТРЫЙ_ЗАПУСК.bat`
2. Сервер будет доступен на `http://localhost:3000`

**Старый способ:**
1. Дважды кликните на файл `запуск.bat` (или `запуск.ps1` для PowerShell)
2. Откройте браузер и перейдите по адресу: `http://localhost:3000`

### Вариант 1: Локальный запуск для тестирования

**Через Python (рекомендуется):**
```bash
# Откройте терминал в папке проекта и выполните:
python -m http.server 8000
```

**Через Node.js:**
```bash
# Если Node.js установлен:
npx http-server -p 8000
```

**Через PHP:**
```bash
php -S localhost:8000
```

**Просто открыть в браузере:**
- Дважды кликните на `index.html`
- ⚠️ Внимание: некоторые функции могут не работать из-за ограничений безопасности браузера (CORS)

2. Откройте браузер и перейдите по адресу:
   ```
   http://localhost:8000
   ```

### Вариант 2: Запуск с сервером для обработки заказов (рекомендуется)

Для полноценной работы с обработкой заказов через Telegram:

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Настройте токен бота:**
   - Токен уже указан в `config.js`
   - Укажите ваш Telegram ID в `config.js` (получите через @userinfobot)

3. **Запустите сервер:**
   ```bash
   npm start
   ```

4. **Настройте бота в BotFather:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/newapp` и выберите вашего бота
   - Укажите URL приложения (для локальной разработки используйте ngrok)

5. **Подробные инструкции:** См. файл `НАСТРОЙКА.md`

### Вариант 3: Только фронтенд (без сервера)

Если нужен только интерфейс без обработки заказов:

1. Разместите файлы на хостинге:
   - [GitHub Pages](https://pages.github.com/)
   - [Netlify](https://www.netlify.com/)
   - [Vercel](https://vercel.com/)

2. Настройте Web App в BotFather с URL вашего хостинга

## Настройка для продакшена

### HTTPS обязателен!

Telegram требует, чтобы Mini App работал только по HTTPS. 

### ⚠️ Важно: Проблема с localtunnel

Localtunnel показывает страницу с предупреждением, что мешает работе Telegram Mini App.

**✅ РЕКОМЕНДУЕТСЯ: Используйте ngrok** - он не показывает предупреждений!

**📖 Подробная инструкция:** См. файл `ИНСТРУКЦИЯ_NGROK.md` или `РЕШЕНИЕ_ПРОБЛЕМЫ_TUNNEL.md`

**Быстрый старт с ngrok:**

1. Установите ngrok: https://ngrok.com/download
2. Зарегистрируйтесь (бесплатно): https://dashboard.ngrok.com/signup
3. Получите токен: https://dashboard.ngrok.com/get-started/your-authtoken
4. Настройте токен: `ngrok config add-authtoken ВАШ_ТОКЕН`
5. Запустите сервер: `npm start` (в первом окне)
6. Запустите ngrok: `ngrok http 3000` (во втором окне)
   - Или дважды кликните на `быстрая_настройка_ngrok.bat`
7. Скопируйте HTTPS URL и используйте в BotFather

**Альтернативы:**

Если ngrok заблокирован (ошибка ERR_NGROK_9040), используйте:

1. **Cloudflare Tunnel (РЕКОМЕНДУЕТСЯ):**
   - Скачайте: https://github.com/cloudflare/cloudflared/releases/latest
   - Переименуйте в `cloudflared.exe` и скопируйте в папку проекта
   - Дважды кликните на `использовать_cloudflared.bat`
   - Или выполните: `cloudflared.exe tunnel --url http://localhost:3000`
   - ✅ Не требует регистрации, не блокируется, без предупреждений

2. **localtunnel:**
   - `npx localtunnel --port 3000`
   - ⚠️ Показывает страницу предупреждения при первом открытии

📖 Подробная инструкция: см. файл `АЛЬТЕРНАТИВА_NGROK.md`

## Кастомизация

### Добавление товаров

Отредактируйте массив `products` в файле `app.js`:

```javascript
const products = [
    {
        id: 1,
        name: "Название товара",
        price: 1000,
        category: "electronics", // или "clothing", "books"
        description: "Описание товара",
        emoji: "📱" // Эмодзи для товара
    },
    // ... другие товары
];
```

### Изменение категорий

В файле `index.html` найдите блок с категориями и измените:

```html
<div class="categories">
    <button class="category-btn active" data-category="all">Все</button>
    <button class="category-btn" data-category="your-category">Ваша категория</button>
</div>
```

### Изменение цветов

В файле `styles.css` измените переменные в `:root`:

```css
:root {
    --primary-color: #3390ec; /* Основной цвет */
    --secondary-color: #2481cc; /* Вторичный цвет */
    /* ... другие цвета */
}
```

## Технические детали

- **Telegram Web App API**: Используется для интеграции с Telegram
- **LocalStorage**: Сохранение корзины между сессиями
- **Адаптивный дизайн**: Работает на мобильных и десктопах
- **Чистый JavaScript**: Без зависимостей и фреймворков

## Поддержка

При возникновении проблем:
1. Убедитесь, что используете HTTPS для продакшена
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что все файлы загружены корректно

## Лицензия

Свободное использование для любых целей.
